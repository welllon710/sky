<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.DishFlavorMapper">


    <insert id="saveFlavors">
        insert into wuwei_sky_take_out.dish_flavor(dish_id, name, value) VALUES
            <foreach collection="flavors" item="item" separator="," >
                (#{item.dishId}, #{item.name}, #{item.value})
            </foreach>
    </insert>

    <delete id="delFlavors">
        delete from wuwei_sky_take_out.dish_flavor d
        where d.dish_id in
            <foreach collection="ids" item="id" separator="," open="(" close=")">
                #{id}
            </foreach>
    </delete>

    <update id="updateFlavors" parameterType="com.sky.entity.DishFlavor">
        UPDATE wuwei_sky_take_out.dish_flavor
        <set>
            <if test="dishId != null">dish_id = #{dishId},</if>
            <if test="name != null">name = #{name},</if>
            <if test="value != null">value = #{value},</if>
        </set>
        WHERE id = #{id}
    </update>

</mapper>